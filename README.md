# Authzed Homebrew Formula

This is the recommended way to install Authzed tooling.

Both macOS and Linux binaries are made available.

Formula include:
- zed
- zed-testserver

## Getting Started

```sh
$ brew tap authzed/tap
$ brew install --HEAD zed
$ brew install zed-testserver
```
